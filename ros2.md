| 功能包                | 作用                                                         |
| --------------------- | ------------------------------------------------------------ |
| `cleanbot_bringup`    | **整机启动入口**。一次启动RTK、下位机、控制、任务、建模、HTTP等节点，并加载启动参数。 |
| `cleanbot_interfaces` | **统一数据格式**。定义Topic消息、Service请求和Action任务格式，本身不处理业务。 |
| `cleanbot_common`     | **公共基础代码**。保存多个节点共同使用的QoS等通用能力。      |
| `cleanbot_config`     | **系统配置中心**。负责参数校验、SQLite持久化和向其他节点提供配置。 |
| `cleanbot_hardware`   | **下位机串口网关**。把ROS2命令编码后发给下位机，并解析下位机状态、ACK和完成反馈。 |
| `cleanbot_rtk`        | **定位与差分链路**。读取RTK、解析经纬度和航向、连接NTRIP、判断固定解、换算车体中心坐标。 |
| `cleanbot_control`    | **车辆运动控制**。计算航向误差、横向偏差和纠偏输出，并从任务、摇杆、视觉、急停命令中选择最终命令。 |
| `cleanbot_mission`    | **任务执行管理**。执行自动清扫和多路点任务，处理转向、直行、暂停、恢复、取消、RTK恢复和触边。 |
| `cleanbot_modeling`   | **打点建模与路径生成**。采集RTK点、识别子区域和连接段、生成往复式清扫路线、保存模型。 |
| `cleanbot_http`       | **本地用户接口**。接收网页或小程序的HTTP请求并转换成ROS2消息。 |
| `cleanbot_gateway`    | **云平台接口**。接收MQTT远程命令，并在云平台消息与ROS2消息之间转换。 |
| `cleanbot_vision`     | **视觉功能预留**。计划负责相机、视觉识别和视觉控制，目前尚未建立。 |
| S9调试平台            | **诊断和测试工具**。计划汇总各节点日志、异常和健康状态，并提供整机测试按钮。 |