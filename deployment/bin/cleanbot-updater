#!/usr/bin/env bash
# 文件作用：通过进程锁串行运行升级器，防止多个升级任务同时修改版本目录。
set -euo pipefail

exec /usr/bin/flock --exclusive --nonblock \
  /run/lock/cleanbot-updater.lock \
  /usr/bin/python3 /usr/local/lib/cleanbot/cleanbot_updater.py "$@"
